//selecciona todos los elementos con el selector dado
const cards = document.querySelectorAll('.card');
console.log(cards);

const noExiste = document.querySelectorAll('noExiste');
console.log(noExiste);