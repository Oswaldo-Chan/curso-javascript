const producto = {
    nombre: 'monitor de 30 pulgadas',
    precio: 300,
    disponible: true
}

const {nombre, precio, disponible} = producto;

console.log(nombre);
console.log(precio);
console.log(disponible);